{
  "name": "esercizi",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "start:pm2": "pm2 start index.js --name backend --time",
    "start:pm2cluster": "pm2 start index.js --name cluster --time -i max",
    "heavy": "loadtest -n 1000 -c 10 http://localhost:3000/heavy"
  },
  "author": "nicola marano <nicoola.marano02@gmail.com>",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "loadtest": "^8.2.0"
  }
}
